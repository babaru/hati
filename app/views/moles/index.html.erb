<h1>Listing moles</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Weibo</th>
    <th>Access token</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moles.each do |mole| %>
  <tr>
    <td><%= mole.name %></td>
    <td><%= mole.weibo_id %></td>
    <td><%= mole.access_token %></td>
    <td><%= link_to 'Show', mole %></td>
    <td><%= link_to 'Edit', edit_mole_path(mole) %></td>
    <td><%= link_to 'Destroy', mole, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mole', new_mole_path %>
