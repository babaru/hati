<%= render_page_title "#{@go.name}" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => Go.model_name.human)}",
    url: gos_path,
    options: {class: 'button'}
  }
end %>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>
        属性
      </th>
      <th>
        值
      </th>
    </tr>
  </thead>
  <tbody>
      </tbody>
  <tfoot>

  </tfoot>
</table>
