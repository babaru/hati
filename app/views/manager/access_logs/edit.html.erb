<%= render_page_title "#{t('model.edit', model: AccessLog.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => AccessLog.model_name.human)}",
    url: manager_access_logs_path,
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
